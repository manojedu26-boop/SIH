# ScrollStack Validation

The revised Hero 7-style signal field now uses four distinct evidence images: duplicate work, execution gap, vendor outlier, and geo mismatch. The desktop first viewport shows a layered evidence stack with the active scan core and visible secondary photo depth. The mobile first viewport preserves the existing hero readability and transitions into the signal field with the vertical swipe/scroll cue; the stack is configured for touch scrolling inside the signal panel. Type check and production build pass after adding Lenis 1.3.26.
